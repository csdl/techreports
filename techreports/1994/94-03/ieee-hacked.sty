%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% -*- Mode: Latex -*- %%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% ieee-hacked.sty -- Style File for IEEE (used for ISPW-94)
%% RCS:            : $Id$
%% Author          : Philip Johnson
%% Created On      : Thu Feb 10 10:52:47 1994
%% Last Modified By: Philip Johnson
%% Last Modified On: Sun Jul 10 13:48:46 1994
%% Status          : Unknown
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%   Copyright (C) 1994 University of Hawaii
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% 
%% History
%% 10-Feb-1994		Philip Johnson	
%%    

%% This is modified from wpi.wpi.edu:/latex/ieee.tex by Matthew Ward, 
%% CS Department, Worcester Polytechnic Institute.

%% Defines two new commands: \makeieeetitle and \makeieeeabstract.
%% You must manually make sure that the abstract is not too long. 

%% First, adjust page parameters.
\setlength{\textheight}{55pc}
\setlength{\columnsep}{2.0pc}
\setlength{\textwidth}{7.0in}
\setlength{\footheight}{0.0in}
\setlength{\topmargin}{-0.15in}
\setlength{\headheight}{0in}
\setlength{\headsep}{0.0in}
\setlength{\oddsidemargin}{-.19in}
\setlength{\parindent}{1pc}

\makeatletter

%% Grunged version of article.sty maketitle command

\def\maketitle{\par
 \begingroup
 \def\thefootnote{\fnsymbol{footnote}}
 \def\@makefnmark{\hbox 
 to 0pt{$^{\@thefnmark}$\hss}} 
 \if@twocolumn 
 \twocolumn[\@maketitle] 
 \else \newpage
 \global\@topnum\z@ \@maketitle \fi\thispagestyle{plain}\@thanks
 \endgroup
 \setcounter{footnote}{0}
 \let\maketitle\relax
 \let\@maketitle\relax
 \gdef\@thanks{}\gdef\@author{}\gdef\@title{}\let\thanks\relax}
\def\@maketitle{\newpage
 \null
 \vspace{0.25in}                   %%% This is the line that is changed.
 \begin{center}
 {\Large \@title \par} \vskip 1em {\large \lineskip .5em
\begin{tabular}[t]{c}\@author 
 \end{tabular}\par} 
% \vskip 1em {\large \@date} 
  \end{center}
 \par
 \vskip 1em
 } 


%%% As Latex considers descenders in its calculation of interline spacing,
%%% To get 12 point spacing for normalsize text, must set it to 10 points
\def\@normalsize{\@setsize\normalsize{12pt}\xpt\@xpt
\abovedisplayskip 10pt plus2pt minus5pt\belowdisplayskip \abovedisplayskip
\abovedisplayshortskip \z@ plus3pt\belowdisplayshortskip 6pt plus3pt
minus3pt\let\@listi\@listI} 

%%% Need an 11 pt font size for subsection and abstract headings
\def\subsize{\@setsize\subsize{12pt}\xipt\@xipt}

%%% Make section titles bold and 12 point, 1 blank lines before, 1 after
\def\section{\@startsection {section}{1}{\z@}{10pt plus 2pt minus 2pt}
{10pt plus 2pt minus 2pt}{\large\bf}}

%%% Make subsection titles bold and 11 point, 1 blank line before, 1 after
\def\subsection{\@startsection {subsection}{2}{\z@}{12pt plus 2pt minus 2pt}
{12pt plus 2pt minus 2pt}{\subsize\bf}}

\newcommand{\makeieeetitle}[2]  %% {Title}{Author} as arguments.
  {\title {{\Large\bf #1}}
   \author {#2}
   \date{}
   \maketitle
  }

\newcommand{\makeieeeabstract}[1]
  {\subsection*{\centering Abstract}
    {\em #1}
  }

\makeatother




